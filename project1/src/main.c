#include <stdio.h>
#include <stdlib.h>
#include <trexio.h>
#include "reader.h"
#include "mol.h"

int main() {

    // initialize mol struct
    Mol m;
    char* path = "./data/h2o.h5";
    mol_init(&m, path);

    // read data from file
    // open file
    

    return 0;
}
